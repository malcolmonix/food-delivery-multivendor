{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Install and run dev GraphQL backend",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-NoProfile",
				"-ExecutionPolicy",
				"Bypass",
				"-Command",
				"cd dev-backend; if (!(Test-Path node_modules)) { npm install }; npm run seed; npm run dev"
			],
			"isBackground": true,
			"problemMatcher": [
				"$eslint - stylish"
			],
			"group": "build"
		},
		{
			"label": "Install and run dev GraphQL backend",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-NoProfile",
				"-ExecutionPolicy",
				"Bypass",
				"-Command",
				"cd dev-backend; if (!(Test-Path node_modules)) { npm install }; npm run seed; npm run dev"
			],
			"isBackground": true,
			"group": "build"
		}
	]
}